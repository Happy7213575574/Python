#import modules
import turtle as tu

# Fullscreen the canvas
screen = tu.Screen()
screen.setup(1.0, 1.0)

#make the turtle
t = tu.Turtle()
t.shape('turtle') # set it's shape
t.speed(50) # set it's speed